# Copyright 2026 NXP
# SPDX-License-Identifier: Apache-2.0
#
# See snippets/pm-device/pm-device.conf for why this is one line. In particular
# CONFIG_PM_S2RAM is deliberately absent: it has no prompt of its own and follows
# from the suspend-to-ram state that pm-dpd.overlay enables. layer.h asserts that
# it came out set.

CONFIG_PM_TEST_LAYER_DPD=y
