# Layer 2: runtime device PM (reference counted).
# pm_device_driver_init() leaves the LPADC SUSPENDED; the app must
# pm_device_runtime_get()/put() around use. Exercises the SUSPEND/RESUME
# callback via the runtime refcount path.

CONFIG_PM_DEVICE=y
CONFIG_PM_DEVICE_RUNTIME=y
