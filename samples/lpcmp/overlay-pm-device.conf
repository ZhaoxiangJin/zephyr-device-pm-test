# Layer 1: manual device PM.
# nxp_lpcmp_pm_callback() (SUSPEND/RESUME) is driven directly through
# pm_device_action_run(). No runtime PM, so pm_device_driver_init() resumes the
# device and it boots ACTIVE.

CONFIG_PM_DEVICE=y
