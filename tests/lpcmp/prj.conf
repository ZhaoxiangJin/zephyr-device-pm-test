# Baseline LPCMP device-PM test: no PM at all.
# Proves the sample brings up the comparator and that the comparator API works,
# so that the PM layers have a control to compare against.

CONFIG_COMPARATOR=y
CONFIG_LOG=y
CONFIG_COMPARATOR_LOG_LEVEL_INF=y
